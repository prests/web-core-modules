import { Button, BUTTON_VARIANT__OUTLINE } from '@lunar-js/lunar';

const HomePage = () => {
  return (
    <main>
      <Button variant={BUTTON_VARIANT__OUTLINE}>Test</Button>
    </main>
  );
};

export default HomePage;
