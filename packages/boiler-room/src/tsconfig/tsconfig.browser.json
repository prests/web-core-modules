// Browser-specific TypeScript configuration extending the base config
// Use this for web applications, Storybook, and browser-targeted libraries
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "display": "WCM Browser",
  "compilerOptions": {
    "target": "es2022",
    "lib": ["es2022", "DOM", "DOM.Iterable"]
  }
}
